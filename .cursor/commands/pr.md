# pr

Checkout a new branch off of main with the name prfefaced with feat-. Move all the changes that were made in this context window to this new branch. The main branch should not have any of these changes. 

Commit the changes using a descriptive commit message and then push the branch up. Return the link to the open PR. 

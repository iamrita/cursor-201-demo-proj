# pr

Take the changes that have been made in the current context window and put them on to a new branch called that's prefaced with feat-.

Add a name for the branch that's related to the work being done. 
Commit the changes using a descriptive commit message and then push the branch up. Return the link to the open PR, and if you can, open the PR using the browser mcp server. 
